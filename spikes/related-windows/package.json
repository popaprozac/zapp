{
  "name": "zapp-related-window-probe",
  "private": true,
  "scripts": {
    "test": "bun run run.ts",
    "test:bridge": "bun run direct-bridge.ts",
    "test:checked-z": "bun run checked-z.ts",
    "test:checked-lifetime": "bun run checked-z.ts --lifetime",
    "test:registry": "bun run registry.ts",
    "test:document-routing": "bun run document-routing.ts",
    "benchmark": "bun run benchmark.ts"
  },
  "dependencies": {
    "react": "19.2.0",
    "react-dom": "19.2.0"
  }
}
