## Kitchen-sink Nim app entry (interim — proves the app-supplied-Nim-root path
## compiles + links + boots). A later task replaces this with the real greet/runApp.
import zapp

quit(zappDefaultMain())
