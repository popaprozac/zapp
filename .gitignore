# Build artifacts
.zapp/
bin/
release/
dist/
*.br

# CEF (webEngine:"chromium") cached binary distribution — ~110 MB minimal dist
# fetched by cli/scripts/fetch-cef.sh (cli/src/cef.ts ensureCefFetched). No
# trailing slash so a symlinked cache (dev shortcut) is ignored too, not just a
# real fetched directory.
vendor/cef

# Dependencies
node_modules/

# Generated
src/zapp/

# IDE
.vscode/
.idea/
.DS_Store

# Environment
.env
.env.local
.env.*.local

# Old v1 code (pending removal)
old/

# Claude
.claude/

# Superpowers SDD scratch (ledger, briefs, reports, review packages)
.superpowers/
