export declare const runBuild: ({ root, frontendDir, buildFile, nativeOut, assetDir, withBrotli, bytecode, }: {
    root: string;
    frontendDir: string;
    buildFile: string;
    nativeOut: string;
    assetDir: string;
    withBrotli: boolean;
    bytecode: boolean;
}) => Promise<void>;
